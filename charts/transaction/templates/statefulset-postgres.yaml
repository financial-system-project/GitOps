{{- if .Values.postgresql.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "transaction-service.fullname" . }}-postgres
  namespace: {{ .Values.Namespace }}
  labels:
    {{- include "transaction-service.labels" . | nindent 4 }}
spec:
  serviceName: {{ include "transaction-service.fullname" . }}-postgres
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "transaction-service.fullname" . }}-postgres
  template:
    metadata:
      labels:
        app: {{ include "transaction-service.fullname" . }}-postgres
    spec:
      containers:
          - name: postgres
            image: "{{ .Values.postgresql.image.repository }}:{{ .Values.postgresql.image.tag }}"
            imagePullPolicy: IfNotPresent
            ports:
              - containerPort: 5432
                name: postgres
            env:
              - name: POSTGRES_DB
                value: {{ .Values.database.name }}
              - name: POSTGRES_USER
                valueFrom:
                  secretKeyRef:
                    name: {{ include "transaction-service.fullname" . }}-secret
                    key: DB_USER
              - name: POSTGRES_PASSWORD
                valueFrom:
                  secretKeyRef:
                    name: {{ include "transaction-service.fullname" . }}-secret
                    key: DB_PASSWORD
              - name: PGDATA
                value: /var/lib/postgresql/data/pgdata
            volumeMounts:
              - name: data
                mountPath: /var/lib/postgresql/data
            resources:
              {{- toYaml .Values.postgresql.resources | nindent 14 }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: {{ .Values.postgresql.storage.size }}
        {{- if .Values.postgresql.storage.storageClass }}
        storageClassName: {{ .Values.postgresql.storage.storageClass }}
        {{- end }}
  {{- end }}