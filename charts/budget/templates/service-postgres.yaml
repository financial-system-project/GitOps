{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "budget-service.fullname" . }}-postgres
  namespace: {{ .Values.Namespace }}
  labels:
    {{- include "budget-service.labels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    app: {{ include "budget-service.fullname" . }}-postgres
  ports:
      - port: {{ .Values.database.port }}
        targetPort: postgres
        name: postgres
  {{- end }}