apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "budget-service.fullname" . }}-config
  namespace: {{ .Values.Namespace }}
data:
  DB_HOST: {{ .Values.database.host }}
  DB_PORT: "{{ .Values.database.port }}"
  SPRING_PROFILES_ACTIVE: "k8s"