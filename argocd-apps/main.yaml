apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: main-app
  namespace: bank-app-dev
spec:
  project: default
  source:
    repoURL: https://github.com/financial-system-project/GitOps.git
    targetRevision: HEAD
    path: ./argocd-apps/microservice-apps
  destination:
    server: https://kubernetes.default.svc
    namespace: bank-app-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

